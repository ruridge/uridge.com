---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Uses | Robert Uridge" description="">
  <h1 class="text-3xl">Work in progress... 🚧👷🚧</h1>
  {["one", "two", "three", "four"].map((project) => <div>{project}</div>)}
</Layout>
